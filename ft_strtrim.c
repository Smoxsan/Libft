char *ft_strtrim(char const *s1, char const *set)
{
    const char* start;
    const char* end;
    size_t new_len;

    if (s1 == NULL) {
        return NULL;
    }
    start = s1;
    end = s1 + ft_strlen(s1) - 1;
    while ()
        end--;
    new_len = end - start + 1;

}